// ╔════════════════════════════════════════════════════════════════════╗
// ║  niri · "Neon Mono"  —  dark cyberpunk × minimal mono                ║
// ║  Behavior re-based on well-tuned community configs so tiling feels   ║
// ║  smooth, not jumpy:                                                  ║
// ║    · niri default config (niri-wm/niri)                             ║
// ║    · github.com/Rouzihiro/dotfiles                                  ║
// ║    · github.com/marcinjahn/dotfiles                                 ║
// ║                                                                      ║
// ║  After editing:  niri validate -c ~/.config/niri/config.kdl          ║
// ║  niri hot-reloads on save. If a line is flagged, comment it (//).    ║
// ╚════════════════════════════════════════════════════════════════════╝

// ── Input ─────────────────────────────────────────────────────────────
input {
    keyboard {
        xkb {
            layout "us"
        }
        repeat-delay 400
        repeat-rate 45
    }

    touchpad {
        tap
        dwt                       // disable-while-typing
        natural-scroll
        accel-speed 0.2
        accel-profile "flat"
    }

    mouse {
        // Sensitivity lever: -1.0 (slowest) … 0.0 (raw) … 1.0 (fastest).
        // You said it was too fast, so we slow it a touch. Nudge to taste.
        accel-speed -0.4
        accel-profile "flat"
    }

    // NOTE: cursor-warp-to-focus and focus-follows-mouse are intentionally
    // OFF — they're a big part of what made it feel "weird/jumpy". Re-enable
    // if you decide you like them:
    // warp-mouse-to-focus mode="center-xy"
    // focus-follows-mouse max-scroll-amount="10%"
}

// ── Outputs ───────────────────────────────────────────────────────────
// Intentionally left to Niri's automatic output configuration. Add explicit
// blocks only after copying exact connector and mode strings from
// `niri msg outputs`; stale hardware names can otherwise blank a display.

// ── Layout ────────────────────────────────────────────────────────────
layout {
    gaps 12
    background-color "transparent"        // let the wallpaper (swww) show through

    // ★ The settings that make scroll-tiling feel sane:
    center-focused-column "on-overflow"   // only scroll when a column would go off-screen
    always-center-single-column           // a lone window sits centered, not stuck left
    default-column-display "normal"

    // Windows open at 2/3 width (felt much less cramped than 1/2).
    default-column-width { proportion 0.66667; }

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
        proportion 0.9
    }
    preset-window-heights {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
        proportion 1.0
    }

    // Signature cyan→violet gradient on the focused window.
    focus-ring {
        width 2
        active-gradient from="#72d9e7" to="#a99cff" angle=45 relative-to="workspace-view"
        inactive-color "#30354a"
    }

    border {
        off
        width 2
        active-gradient from="#72d9e7" to="#a99cff" angle=45 relative-to="workspace-view"
        inactive-color "#30354a"
    }

    shadow {
        on
        softness 28
        spread 4
        offset x=0 y=8
        color "#080910cc"
    }

    // Where a dragged window will drop.
    insert-hint {
        color "#a99cff55"
    }

    // Neon tabs for tabbed columns (Mod+W).
    tab-indicator {
        hide-when-single-tab
        place-within-column
        gap 6
        width 4
        position "left"
        corner-radius 8
        active-color "#a99cff"
        inactive-color "#555d7c"
    }

    struts { }
}

prefer-no-csd

screenshot-path "~/Pictures/Screenshots/Screenshot-%Y%m%d-%H%M%S.png"

hotkey-overlay { skip-at-startup; }

cursor {
    xcursor-size 24
    // install 'bibata-cursor-theme', then: xcursor-theme "Bibata-Modern-Ice"
}

// ── Environment ───────────────────────────────────────────────────────
environment {
    QT_QPA_PLATFORM "wayland;xcb"
    QT_WAYLAND_DISABLE_WINDOWDECORATION "1"
    MOZ_ENABLE_WAYLAND "1"
    ELECTRON_OZONE_PLATFORM_HINT "auto"
    SDL_VIDEODRIVER "wayland"
    _JAVA_AWT_WM_NONREPARENTING "1"
    GDK_BACKEND "wayland,x11"
}

// ── Autostart ─────────────────────────────────────────────────────────
// Tonantzintla is the Niri shell. Resolve the installed controller through HOME;
// display-manager sessions do not always import ~/.local/bin into PATH.
spawn-at-startup "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" session-environment"
spawn-at-startup "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" session-start"
// Idle locking is owned by the shell (Settings → Lock screen), including when
// installing with --niri keep. This optional watcher handles display power and
// requests a lock before sleep; suspend readiness needs separate verification.
spawn-at-startup "swayidle" "-w" "timeout" "600" "niri msg action power-off-monitors" "resume" "niri msg action power-on-monitors" "before-sleep" "$HOME/.local/bin/blackhole lock"
// Clipboard capture will be enabled when Transit owns clipboard history.
// spawn-at-startup "wl-paste" "--watch" "cliphist" "store"
spawn-at-startup "/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1"
// For X11 apps (Steam, some games): install 'xwayland-satellite', then:
// spawn-at-startup "xwayland-satellite"

// ── Animations ────────────────────────────────────────────────────────
animations {
    // slowdown 1.0
    window-open  { duration-ms 200; curve "ease-out-expo"; }
    window-close { duration-ms 150; curve "ease-out-quad"; }
    window-movement          { spring damping-ratio=0.92 stiffness=700 epsilon=0.0001; }
    window-resize            { spring damping-ratio=1.0  stiffness=800 epsilon=0.0001; }
    horizontal-view-movement { spring damping-ratio=0.95 stiffness=700 epsilon=0.0001; }
    workspace-switch         { spring damping-ratio=1.0  stiffness=900 epsilon=0.0001; }
    overview-open-close      { spring damping-ratio=0.9  stiffness=700 epsilon=0.0001; }
    config-notification-open-close { spring damping-ratio=0.7 stiffness=900 epsilon=0.001; }
}

// ── Window rules ──────────────────────────────────────────────────────
window-rule {
    geometry-corner-radius 10
    clip-to-geometry true
}

window-rule {
    match app-id=r#"^(pavucontrol|org\.pulseaudio\.pavucontrol|blueman-manager|nm-connection-editor|org\.gnome\.Calculator|file-roller)$"#
    open-floating true
}

window-rule {
    match title="^Picture-in-Picture$"
    match title="^Picture in picture$"
    open-floating true
}

// ── Keybinds ──────────────────────────────────────────────────────────
// Mod = Super. Full list any time with  Mod+Shift+/
binds {
    Mod+Shift+Slash { show-hotkey-overlay; }

    // Tonantzintla desktop keys. Hyprland remains reference-only.
    Alt+F4            { close-window; }
    Mod+Return        { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" terminal"; }
    Mod+D             { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" toggle apps"; }
    Mod+E             { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" files"; }
    Mod+B             { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" browser"; }
    Mod+Shift+Escape  { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" toggle system"; }
    Mod+Shift+N       { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" toggle settings"; }
    Mod+Shift+Q       { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" quick telemetry"; }
    // Native Niri crop UI: select a region, then copy it to the clipboard.
    // Optics Bay keeps the separate Slurp/Grim capture workflows.
    Mod+Shift+S repeat=false { screenshot; }
    Super+Alt+L       { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" lock"; }
    Mod+Shift+W       { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" toggle walls"; }
    Mod+Shift+C       { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" toggle clipboard"; }
    Mod+Shift+A       { spawn "sh" "-lc" "exec \"$HOME/.local/bin/blackhole\" toggle notifications"; }

    // Alt-tab: jump straight back to the last window you were on (MRU), no mouse
    Mod+Tab           repeat=false { focus-window-previous; }

    // Window / column management
    Mod+Q             { close-window; }
    Mod+F             { maximize-column; }
    Mod+Shift+F       { fullscreen-window; }
    Mod+C             { center-column; }
    Mod+V             { toggle-window-floating; }
    Mod+Shift+V       { switch-focus-between-floating-and-tiling; }
    Mod+W             { toggle-column-tabbed-display; }
    Mod+R             { switch-preset-column-width; }
    Mod+Shift+R       { switch-preset-window-height; }

    // Focus — horizontal arrows; hjkl retains full directional focus
    Mod+Left  { focus-column-left; }
    Mod+Right { focus-column-right; }
    Mod+H     { focus-column-left; }
    Mod+L     { focus-column-right; }
    Mod+K     { focus-window-up; }
    Mod+J     { focus-window-down; }
    Mod+Home  { focus-column-first; }
    Mod+End   { focus-column-last; }

    // Move
    Mod+Ctrl+Left  { move-column-left; }
    Mod+Ctrl+Right { move-column-right; }
    Mod+Ctrl+Up    { move-window-up; }
    Mod+Ctrl+Down  { move-window-down; }
    Mod+Ctrl+H     { move-column-left; }
    Mod+Ctrl+L     { move-column-right; }
    Mod+Ctrl+K     { move-window-up; }
    Mod+Ctrl+J     { move-window-down; }

    // Move windows/columns between monitors
    Mod+Shift+Left  { move-column-to-monitor-left; }
    Mod+Shift+Right { move-column-to-monitor-right; }

    // Build/break columns
    Mod+Comma        { consume-window-into-column; }
    Mod+Period       { expel-window-from-column; }
    Mod+BracketLeft  { consume-or-expel-window-left; }
    Mod+BracketRight { consume-or-expel-window-right; }

    // Sizing
    Mod+Minus       { set-column-width "-10%"; }
    Mod+Equal       { set-column-width "+10%"; }
    Mod+Shift+Minus { set-window-height "-10%"; }
    Mod+Shift+Equal { set-window-height "+10%"; }

    // Workspaces & overview
    Mod+O         { toggle-overview; }
    Mod+Down      { focus-workspace-down; }
    Mod+Up        { focus-workspace-up; }
    Mod+Page_Down { focus-workspace-down; }
    Mod+Page_Up   { focus-workspace-up; }
    Mod+Ctrl+Page_Down { move-column-to-workspace-down; }
    Mod+Ctrl+Page_Up   { move-column-to-workspace-up; }

    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }
    Mod+0 { focus-workspace 10; }
    Mod+Ctrl+1 { move-column-to-workspace 1; }
    Mod+Ctrl+2 { move-column-to-workspace 2; }
    Mod+Ctrl+3 { move-column-to-workspace 3; }
    Mod+Ctrl+4 { move-column-to-workspace 4; }
    Mod+Ctrl+5 { move-column-to-workspace 5; }
    Mod+Ctrl+6 { move-column-to-workspace 6; }
    Mod+Ctrl+7 { move-column-to-workspace 7; }
    Mod+Ctrl+8 { move-column-to-workspace 8; }
    Mod+Ctrl+9 { move-column-to-workspace 9; }
    Mod+Ctrl+0 { move-column-to-workspace 10; }

    // Screenshots. Meta+Shift+S is the Windows-style clipboard snipper.
    Print      { screenshot; }
    Ctrl+Print { screenshot-screen; }
    Alt+Print  { screenshot-window; }

    // Direct backends until Tonantzintla owns the OSD.
    XF86AudioRaiseVolume  allow-when-locked=true { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+ -l 1.0"; }
    XF86AudioLowerVolume  allow-when-locked=true { spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"; }
    XF86AudioMute         allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"; }
    XF86AudioMicMute      allow-when-locked=true { spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"; }
    XF86AudioPlay         allow-when-locked=true { spawn "playerctl" "play-pause"; }
    XF86AudioNext         allow-when-locked=true { spawn "playerctl" "next"; }
    XF86AudioPrev         allow-when-locked=true { spawn "playerctl" "previous"; }
    XF86MonBrightnessUp   allow-when-locked=true { spawn "brightnessctl" "set" "+5%"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "brightnessctl" "set" "5%-"; }

    // Session
    Mod+Shift+E     { quit; }
    Ctrl+Alt+Delete { quit; }
    Mod+Shift+P     { power-off-monitors; }
}
