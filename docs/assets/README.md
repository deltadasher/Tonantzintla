# Website captures

These images and the silent 13-second MP4 are extracted from the maintainer's
2026-09-12 desktop recording. They show the actual recorded shell, not a browser
simulation. The Umbra page uses the existing brand illustration instead of
presenting an unrecorded lock screen as a product capture.

The MP4 has no audio track and loads only when the visitor requests playback.
